{
  "name": "Laravel Pharmacy Management",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/var/www/html",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.vscode-php",
        "bmewburn.vscode-intelephense-client",
        "formulahendry.auto-rename-tag",
        "bradlc.vscode-tailwindcss"
      ]
    }
  },
  "forwardPorts": [8000],
  "postCreateCommand": "composer install && cp .env.example .env && php artisan key:generate && php artisan migrate --force && php artisan db:seed --force"
}
